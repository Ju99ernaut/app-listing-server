//// -- LEVEL 1
//// -- Tables and References

// Creating tables
Table users as U {
  id int [pk, increment] // auto-increment
  username varchar [not null, unique]
  email varchar [not null, unique]
  password varchar
  disabled bool
}

Table applications {
  id int [pk, increment] // auto-increment
  image varchar
  title varchar [not null, unique]
  by varchar
  rating float [ref: - rating.rating]
  groups [varchar]
  owner varchar [ref: - U.username]
}

// Single rating per application per user
Table ratings {
  id int [pk, increment] // auto-increment
  user varchar [ref: - U.username]
  application varchar [ref: - applications.title]
  rating float
}

// Overall rating per application
Table rating {
  id int [pk, increment] // auto-increment
  application varchar [ref: - applications.title]
  rating float
}

// Creating references
// You can also define relaionship separately
// > many-to-one; < one-to-many; - one-to-oe) > merchants.(id, country_code)
